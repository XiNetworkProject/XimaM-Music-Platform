import fs from 'node:fs/promises';
import path from 'node:path';

const base = path.resolve('artifacts/chambre-signature');
const files = (await fs.readdir(path.join(base, 'captures'))).filter(file => /^[a-z0-9-]+\.png$/.test(file)).sort();
const escape = text => text.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');
const labels = {create:'L’atelier',publish:'Préparer une sortie',community:'Les rencontres',search:'Trouver sa piste',support:'À ton écoute',spaces:'L’atlas des espaces',library:'La bibliothèque',notifications:'Les échos'};
const title = file => `${labels[file.split('-')[0]] || 'Synaura'} · ${file.includes('390') ? '390 × 844' : '1440 × 900'}${file.includes('guest') ? ' · état invité' : ''}${file.includes('guide') ? ' · le parcours' : ''}${file.includes('clubs') ? ' · les clubs' : ''}${file.includes('form') ? ' · formulaire' : ''}`;
const entries = files.map(file => `<figure class="${file.includes('390') ? 'mobile' : 'desktop'}"><a href="captures/${file}"><img loading="lazy" src="captures/${file}" alt="${escape(title(file))}"></a><figcaption>${escape(title(file))}</figcaption></figure>`).join('');
await fs.writeFile(path.join(base,'review.html'), `<!doctype html><html lang="fr"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Synaura — Signature Chambre</title><style>
*{box-sizing:border-box}body{margin:0;color:#f3f6fc;background:#030508;font:14px/1.65 Arial,Helvetica,sans-serif}main{max-width:1500px;margin:auto;padding:clamp(22px,5vw,72px)}header{border-bottom:1px solid #283346;padding-bottom:36px;margin-bottom:40px}small{color:#86afff;letter-spacing:.16em}h1{font-size:clamp(48px,8vw,116px);line-height:.9;letter-spacing:-.075em;font-weight:750;margin:30px 0}h1 span{color:#86afff}p{max-width:80ch;color:#a9b5c9}a{color:inherit}nav{display:flex;gap:12px;flex-wrap:wrap;margin-top:28px}nav a{padding:12px 18px;border:1px solid #283346;text-decoration:none;min-height:44px}nav a:hover{background:#14213c;border-color:#86afff}a:focus-visible{outline:2px solid #b6d4ff;outline-offset:4px}.gallery{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:26px}figure{margin:0;border-top:1px solid #283346;padding-top:14px;min-width:0}.desktop{grid-column:span 4}.mobile{grid-column:span 2}img{display:block;width:100%;max-height:670px;object-fit:contain;object-position:top;background:#070a10}figcaption{padding-top:14px;color:#a9b5c9;font-size:12px}footer{margin-top:50px;border-top:1px solid #283346;padding-top:24px}@media(max-width:800px){.gallery{grid-template-columns:1fr}.desktop,.mobile{grid-column:auto}img{max-height:780px}}@media(prefers-reduced-motion:no-preference){nav a{transition:background-color .18s,border-color .18s}}
</style><main><header><small>SYNAURA / CANDIDATE LOCALE</small><h1>Une identité.<br><span>Des mondes.</span></h1><p>Atelier, publication, recherche, collections, communauté, activité et services : la signature Chambre continue dans le produit. L’entrée approuvée reste intacte.</p><p>Ces captures montrent les états réellement accessibles en local. Les vues invitées ne valident pas les comptes connectés ; les listes de clubs locales ne prouvent pas le contenu de production. Aucun contenu fictif n’a été publié et aucun achat n’a été réalisé.</p><nav><a href="http://127.0.0.1:3000/create">Explorer l’atelier ↗</a><a href="http://127.0.0.1:3000/publish">Préparer une sortie ↗</a><a href="http://127.0.0.1:3000/community">Les rencontres ↗</a><a href="http://127.0.0.1:3000/search">La recherche ↗</a><a href="../chambre-continuation/review.html">Live, Découvrir & récompenses ↗</a></nav></header><section class="gallery" aria-label="Captures vérifiées">${entries}</section><footer><p>${files.length} captures. Aucun commit ni déploiement. <a href="../../docs/chambre-signature-validation.md">Vérifications et réserves</a>.</p></footer></main></html>`);
console.log(JSON.stringify({gallery:path.join(base,'review.html'),captures:files.length}));
